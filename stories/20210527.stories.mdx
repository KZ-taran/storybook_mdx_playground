import { Meta } from "@storybook/addon-docs/blocks";

<Meta title="Category/title" />

# SamplePage

plz start the NextJS server and the Storybook server.

```bash
$ yarn dev  # serve some public images.
$ yarn storybook
```

This is Storybook thumbnail

![html](http://localhost:3000/storybook.svg "storybook")
